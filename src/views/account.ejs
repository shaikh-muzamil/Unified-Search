<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Account - Unified Search</title>
    <link rel="stylesheet" href="/style.css">
</head>

<body>
    <%- include('partials/header', { user: user }) %>
        <div class="container">
            <h1>Account Settings <small style="font-size: 0.5em; color: #888;">(v2.0)</small></h1>
            <div class="card">
                <h2>Profile</h2>
                <p><strong>Name:</strong>
                    <%= user.name %>
                </p>
                <p><strong>Email:</strong>
                    <%= user.email %>
                </p>
            </div>

            <div class="card">
                <h2>Integrations</h2>
                <div class="integration-status">
                    <div class="status-item">
                        <span>Slack</span>
                        <% if (slackConnected) { %>
                            <span class="badge success">Connected</span>
                            <% } else { %>
                                <a href="/auth/slack" class="button button-small">Connect Slack</a>
                                <% } %>
                    </div>
                    <div class="status-item">
                        <span>Notion</span>
                        <% if (notionConnected) { %>
                            <span class="badge success">Connected</span>
                            <% } else { %>
                                <a href="/auth/notion" class="button button-small">Connect Notion</a>
                                <% } %>
                    </div>
                </div>
            </div>
        </div>
</body>

</html>